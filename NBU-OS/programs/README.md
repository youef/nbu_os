# NBU-OS programs

Programs stored here belong to the NBU-OS application ecosystem. They are not Linux binaries.

The current runtime has one registered program: `/system/hello`, implemented in the kernel prototype. Future applications will be packaged as signed `NBUX` files and installed below `/apps`.
